# Backend Dockerfile - Development (sem build)
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy all source
COPY . .

# Expose port
EXPOSE 3001

# Start in dev mode
CMD ["npm", "run", "dev"]
